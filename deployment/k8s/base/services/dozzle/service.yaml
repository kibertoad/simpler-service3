apiVersion: v1
kind: Service
metadata:
  name: dozzle
  labels:
    app.kubernetes.io/name: dozzle
    app.kubernetes.io/component: observability
spec:
  type: NodePort
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 8888
  selector:
    app.kubernetes.io/name: dozzle
