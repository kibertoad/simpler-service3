apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

# Secret consumed by the `app` Deployment. Vault writes one .env.simpler-service3
# (the app env) which the deploy script copies in here.
secretGenerator:
  - name: simpler-service3-secret
    behavior: create
    envs:
      - .env
